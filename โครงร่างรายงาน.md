# โครงร่างรายงานโครงงาน — บทที่ 3 และ 4 (สำหรับผู้รับผิดชอบฝั่ง Backend)

> โครงร่างนี้จัดทำสำหรับการแบ่งงาน 2 คน (Frontend / Backend) โดยเนื้อหาส่วนนี้เน้น **ฝั่ง Backend**
> หลักการเขียน: **บทที่ 3 = เล่ากระบวนการ (ทำยังไงทีละขั้น) · บทที่ 4 = เล่าผลลัพธ์ (เสร็จแล้วได้อะไร + พิสูจน์ว่าทำงานได้)**
> ของที่กล่าวในบท 3 ทุกข้อ ควรมี "ผลลัพธ์/หลักฐาน" มาปรากฏต่อในบท 4

---

## โครงสร้างเล่มทั้งฉบับ (ภาพรวม)

1. บทที่ 1 บทนำ — ที่มาและความสำคัญ, วัตถุประสงค์, ขอบเขต, ประโยชน์ที่คาดว่าจะได้รับ, นิยามศัพท์
2. บทที่ 2 แนวคิด ทฤษฎี และงานวิจัยที่เกี่ยวข้อง — (ฝั่ง backend: Express, MySQL, ความปลอดภัย; เพื่อนรับผิดชอบ: Frontend)
3. บทที่ 3 วิธีการดำเนินงาน — **ส่วนนี้**
4. บทที่ 4 ผลการดำเนินงาน — **ส่วนนี้**
5. บทที่ 5 สรุปผล อภิปรายผล และข้อเสนอแนะ

---

# บทที่ 3 วิธีการดำเนินงาน

## 3.1 ขั้นตอนการดำเนินงานโดยรวม

**ให้เขียน:**
- ระบุขั้นตอนหลักเป็น timeline/flowchart: วิเคราะห์ความต้องการ → ออกแบบระบบ → พัฒนา → ทดสอบ → ติดตั้งและใช้งานจริง
- ระบุขอบเขตการรับผิดชอบ: *"ระบบงานประกอบด้วย 2 ส่วนหลักคือ Frontend และ Backend โดยผู้วิจัยรับผิดชอบและพัฒนาฝั่ง Backend ซึ่งครอบคลุมตั้งแต่การออกแบบฐานข้อมูล นำเข้าข้อมูลจาก Excel จัดการสิทธิ์ผู้ใช้ และความปลอดภัยของระบบ ส่วนหน้าจอแสดงผลพัฒนาโดยผู้ร่วมวิจัย"*

## 3.2 การวิเคราะห์ความต้องการระบบ (ข้อมูลจากบทที่ 1)

**ให้เขียน:**

### 3.2.1 Analysis — วิเคราะห์ผู้ใช้ระบบ
| ผู้ใช้งาน | หน้าที่ / ความต้องการ |
|---|---|
| เจ้าหน้าที่ทั่วไป (user) | ดู ค้นหา ส่งออกข้อมูล พิมพ์ป้ายสินทรัพย์ เข้าถึงเอกสารได้ |
| เจ้าหน้าที่/ผู้ดูแล (admin) | + นำเข้า/อัปเดตข้อมูลจาก Excel, ย้ายแผนก, ดู log, จัดการเอกสารแผนก |
| ผู้ดูแลสูงสุด (super_admin) | + แก้ไข/ลบ/เปลี่ยนสถานะสินทรัพย์, จัดการผู้ใช้/แผนก/หมวด |

### 3.2.2 Functional Requirements (FR) ที่เกี่ยวกับ Backend
- ระบบสามารถนำเข้าและอัปเดตข้อมูลสินทรัพย์จากไฟล์ Excel ได้
- ระบบสามารถบันทึกประวัติการย้ายแผนกของสินทรัพย์ได้
- ระบบต้องมีสิทธิ์การเข้าถึงแยกตามบทบาท (role-based)
- ระบบต้องบันทึก Log การกระทำของผู้ใช้เพื่อการตรวจสอบย้อนหลัง
- ระบบต้องรองรับการจัดเก็บและดาวน์โหลดเอกสาร PDF แยกตามแผนก

### 3.2.3 Use Case และตารางสิทธิ์
- แทรก Use Case Diagram + ตารางสรุปสิทธิ์ (ดูใน `ระบบอธิบาย.md`)

## 3.3 การออกแบบระบบฝั่ง Backend

**ให้เขียน:**

### 3.3.1 สถาปัตยกรรมระบบ (3-Tier / MVC)
- รูปแผนภาพ: ระดับ Presentation (EJS/Bootstrap) → Application (Express: Route → Controller → Service/Model) → Data (MySQL)
- อธิบาย Flow: Browser → Nginx (HTTPS) → Express → Middleware chain → Controller → Model → MySQL → Response

### 3.3.2 โครงสร้างโปรเจกต์
- แทรก tree โฟลเดอร์ (app.js, config/db.js, middleware, routes, controllers, models, helpers, views, public, locales)

### 3.3.3 การออกแบบฐานข้อมูล
- ER Diagram + ความสัมพันธ์ระหว่างตาราง (user, asset, department, category, activity_log, asset_transfer, document)
- ตาราง Schema ครบทุกตารางพร้อมคำอธิบายคอลัมน์ (ดูใน `ระบบอธิบาย.md`)
- อธิบายการ Migration อัตโนมัติที่ `config/db.js` (ALTER TABLE ตอนเริ่มระบบ)

### 3.3.4 การออกแบบ Middleware
- การตรวจยืนยันตัวตนและสิทธิ์: requireAuth / requireAdminOrSuperAdmin / requireSuperAdmin
- การป้องกัน CSRF ด้วย token
- การจำกัดจำนวนครั้งการเข้าสู่ระบบ (rate-limit)
- ระบบภาษา (i18n) ผ่าน middleware locale

## 3.4 เครื่องมือและเทคโนโลยีที่ใช้

| เครื่องมือ/เทคโนโลยี | เวอร์ชัน | ใช้ทำอะไร |
|---|---|---|
| Node.js | v1x/20.x | runtime ฝั่งเซิร์ฟเวอร์ |
| Express | 5.2.1 | framework เว็บแอปพลิเคชัน (Routing / Middleware) |
| MySQL | 8.x | ฐานข้อมูลเก็บสินทรัพย์ ผู้ใช้ และ Log |
| EJS | 3.x | template server-side ร่วมกับ Frontend |
| bcrypt | - | เข้ารหัสรหัสผ่าน |
| multer | - | รับ/จัดการไฟล์อัปโหลด (Excel, PDF, รูปภาพ) |
| xlsx | - | อ่านและแปลงข้อมูลจาก Excel |
| nodemailer | - | ส่งอีเมลยืนยันตัวตน / รีเซ็ตรหัสผ่าน |
| helmet | - | ความปลอดภัย HTTP header / CSP |
| express-session + express-mysql-session | - | จัดการ session เก็บในฐานข้อมูล |
| GCP Compute Engine + Nginx + systemd | - | โฮสต์/รีเวิร์สพร็อกซี/บริการอัตโนมัติของเว็บ |

## 3.5 การพัฒนาและประกอบระบบ Backend

**ให้เขียน (เรียงตามโมดูลที่พัฒนาจริง):**

### 3.5.1 การเตรียมสภาพแวดล้อมและติดตั้งบนเซิร์ฟเวอร์
- สร้าง VM (GCP Compute Engine), ติดตั้ง Node.js + MySQL, ติดตั้ง Nginx reverse proxy + HTTPS
- จัดการบริการด้วย systemd (`/opt/assetmanage`, user `assetapp`) และ Git pull เพื่อ deploy

### 3.5.2 โมดูลระบบย่อย (ทีละโมดูล — อธิบายการทำงานโดยอ้างอิงทฤษฎีในบทที่ 2)
1. **ระบบสมาชิกและยืนยันตัวตน** — Register (ตรวจสอบความซับซ้อนรหัสผ่าน + reCAPTCHA + ยืนยันอีเมล), Login (bcrypt compare + rate-limit), Reset password, จัดการโปรไฟล์
2. **ระบบนำเข้าข้อมูล Excel (Upload)** — multer รับไฟล์ → parse และจับคู่หัวคอลัมน์ (excelParser) → แสดง Preview → INSERT แบบ upsert + บันทึก Log
3. **ระบบอัปเดตข้อมูลจาก Excel (Update)** — parse → เทียบข้อมูลกับฐานข้อมูล → แยก changed/toCreate/unchanged → ให้เลือก new/old ทีละคอลัมน์ → นำไปอัปเดต
4. **ระบบย้ายแผนกสินทรัพย์ (Transfer)** — ค้นหา/เลือกหลายรายการ → เปลี่ยนแผนก + บันทึกประวัติการย้าย
5. **ระบบจัดการเอกสาร (Documents)** — อัปโหลด PDF กำหนดแผนก → ดู/ดาวน์โหลด → ลบตามสิทธิ์
6. **ระบบบันทึก Log การใช้งาน** — บันทึก action ของทุกโมดูล + ดูรายละเอียดแบบ batch
7. **ระบบจัดการข้อมูลพื้นฐาน (Admin)** — จัดการผู้ใช้/แผนก/หมวด (เพิ่ม แก้ไข ลบ นำเข้า Excel)

### 3.5.3 การจัดการความปลอดภัย
- สรุปที่มาครอบคลุม: bcrypt, CSRF token, helmet/CSP, rate-limit, role-based access, การ escape ค่าในหน้าจอ, session เก็บในฐานข้อมูล

## 3.6 การออกแบบการทดสอบระบบ

- เลือกวิธี Black-box Testing (ทดสอบผ่านหน้าจอ/API จริง) ตามความต้องการใช้งาน
- ออกแบบตาราง Test Case ล่วงหน้า (รหัส TC-xx) — ผลจริงไปกรอกในบทที่ 4

**Template ตาราง Test Case:**
| รหัส | ฟังก์ชันที่ทดสอบ | ขั้นตอน/เงื่อนไข | ผลที่คาดหวัง | ผลจริง | ผลลัพธ์ |
|---|---|---|---|---|---|
| TC-01 | เข้าสู่ระบบ | ใส่ Username/Password ถูกต้อง | เข้าสู่ระบบสำเร็จ ไปหน้า Dashboard | | ✅/❌ |
| TC-02 | เข้าสู่ระบบ | ใส่รหัสผิด 5 ครั้ง | ระบบหน่วง/บล็อกชั่วคราว | | ✅/❌ |
| TC-03 | สิทธิ์เข้าถึง | User เข้า URL /upload | ถูกปฏิเสธ/redirect | | ✅/❌ |
| TC-04 | Upload Excel | ไฟล์ถูกต้อง 10 แถว | Preview 10 รายการ → นำเข้า 10 | | ✅/❌ |
| TC-05 | Upload Excel | ไฟล์ผิดรูปแบบ | แจ้งข้อผิดพลาด ไม่ทำงานต่อ | | ✅/❌ |
| TC-06 | Update Excel | ไฟล์มีทั้งรายการเดิม/ใหม่ | diff แสดง → เลือก new/old → อัปเดตถูกต้อง | | ✅/❌ |
| TC-07 | Transfer | ย้าย 5 รายการไปแผนก B | dept_name เปลี่ยน + มีประวัติ 5 รายการ | | ✅/❌ |
| TC-08 | CSRF | ส่ง POST ไม่มี token | ตอบกลับ 403 | | ✅/❌ |

---

# บทที่ 4 ผลการดำเนินงาน

## 4.1 ผลการพัฒนาระบบ (ภาพรวม)

**ให้เขียน:**
- สรุปว่าพัฒนาโมดูลสำเร็จทั้งหมดกี่โมดูล/ครอบคลุมฟังก์ชันหลักครบตาม FR ในบทที่ 3
- แผนภาพสถาปัตยกรรมที่ได้จริง (Server, DB, โครงสร้างโปรเจกต์) + เทคโนโลยีที่ใช้จริง

## 4.2 ผลการทดสอบระบบ (หัวใจสำคัญของบทนี้)

**ให้เขียน:**
- นำตาราง Test Case จากบทที่ 3 มากรอก "ผลจริง" และ "ผลลัพธ์"
- ครอบคลุมโมดูล Backend: ระบบสมาชิก, Upload, Update, Transfer, Logs, Admin, การทดสอบสิทธิ์, การทดสอบความปลอดภัย
- แทรก **ภาพหน้าจอ** ทุกหน้าสำคัญ + คำอธิบายใต้ภาพ
- เช่น ตัวอย่างผลการทดสอบระบบสมาชิก:

| รหัส | กรณีทดสอบ | ผลที่คาดหวัง | ผลจริง | ผลลัพธ์ |
|---|---|---|---|---|
| TC-01 | Login ด้วยข้อมูลถูกต้อง | เข้า Dashboard | เข้า Dashboard ได้ | ✅ ผ่าน |

> จากผลการทดสอบทั้งหมด X กรณี พบว่าผ่านทั้ง Y กรณี คิดเป็น 100% (ถ้ามีไม่ผ่านให้อธิบาย)
> กรณีข้อมูลจริง 3,000 รายการ — ทดสอบการค้นหา/หน้า/ส่งออกใช้เวลาไม่เกิน X วินาที (ถ้ามีการวัด)

## 4.3 ผลการทดสอบด้านความปลอดภัย (ถ้ามี)

- สรุปว่าผ่าน: มี CSRF token, ไม่พบ error 403 เมื่อไม่ส่ง token, ผู้ใช้ไม่มีสิทธิ์เข้า URL เกินสิทธิ์ (ทดสอบด้วย role ต่ำ)

## 4.4 สรุปผลตามวัตถุประสงค์

**ตารางเทียบ "วัตถุประสงค์ (บทที่ 1) ↔ ผลลัพธ์ (บทที่ 4):"**

| วัตถุประสงค์ | ผลการดำเนินงาน | บรรลุ |
|---|---|---|
| 1. พัฒนาระบบจัดการสินทรัพย์ | พัฒนาโมดูลครบ X โมดูล | ✅ |
| 2. นำเข้า/อัปเดตข้อมูลจาก Excel | ระบบรับไฟล์/ตรวจ/นำเข้าได้ | ✅ |
| 3. จัดการสิทธิ์ผู้ใช้ | แยกสิทธิ์ 3 ระดับทดสอบแล้ว | ✅ |
| 4. ตรวจสอบย้อนหลังได้ | มี Log และประวัติการย้าย | ✅ |

---

## เคล็ดลับการแบ่งเล่มกับเพื่อน (Frontend/Backend)

| เรื่อง | เพื่อน (Frontend) | คุณ (Backend) |
|---|---|---|
| บทที่ 2 ทฤษฎี | Bootstrap/CSS/JavaScript/UX | Express/MySQL/ความปลอดภัย |
| บทที่ 3 ออกแบบ | หน้าจอ/ส่วนประกอบ/UX/Responsive | สถาปัตยกรรม/DB/Middleware/API |
| บทที่ 4 ทดสอบ | ความถูกต้องหน้าจอ/การใช้งานจริง | ฟังก์ชัน/สิทธิ์/ความปลอดภัย/ข้อมูล |

**ข้อควรระวัง:**
- ห้ามเขียนซ้ำกันทั้งเล่ม — แต่ละคน "เล่า + ทดสอบ" เฉพาะส่วนที่รับผิดชอบ
- ถ้าต้องพูดถึงอีกฝ่าย ให้เกริ่นสั้น ๆ เช่น *"หน้าเข้าสู่ระบบสร้างโดยฝั่ง Frontend ซึ่งทำการส่งข้อมูลไปยัง API `/auth/login` ที่พัฒนาขึ้นในงานวิจัยนี้"*
- ทำให้ทุกหัวข้อในบท 3 มีหลักฐานในบท 4 (รูป/ตารางผลทดสอบ) เสมอ